number of memeber
<%= @count%><br>
<%if !@eventmember.present? and current_user.isadmin != "admin" and  !@eventadmin.present? %>
<%= button_to 'join'  ,{:controller => "events", :action => "action", :event_id => @event.id, :user_id=>current_user.id ,:group_id=>@group.id} ,:id=>"button", :method=>:post  %>
<% end %>



<div class="container">
<%= image_tag @event.avatar.url(:medium) %>
<p>
  <strong>Date:</strong>
  <%= @event.date %>
</p>
<p>
  <strong>Time:</strong>
  <%= @event.time %>
</p>

<p>
  <strong>Location:</strong>
  <%= @event.location %>
</p>

<p>
  <strong>Title:</strong>
  <%= @event.title %>
</p>
 
<p>
  <strong>description:</strong>
  <%= @event.description %>
</p>


<h2>comments</h2>
<% @event.comments.each do |comment| %>
  
 
  <p>
    <strong>Comments:</strong>
    <%= comment.text %>



<%= link_to 'Delete ', [@group,@event, comment],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>


<%= link_to 'Edit', edit_group_event_comment_path(@group,@event,comment), method: :get %>



<% end %>

<h2>Add a comment:</h2>
<%= form_for([@group,@event,@event.comments.build]) do |f| %>


  <div class="form-group">
    <%= f.label :body %><br>
    <%= f.text_area :text ,class:"form-control"%>
  </div> 
  <div class="col-sm-10">
    <%= f.submit %>
 </div>
<% end %>
</div>
